# Python dependencies for the FastAPI backend.
# Lives at the repo root because Vercel's Python runtime installs from here.
# Local setup: cd backend && pip install -r ../requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
motor==3.6.0
certifi==2024.12.14
beanie==1.27.0
PyJWT==2.10.1
Pillow==11.0.0
httpx==0.27.2
